# src/main/resources/application.properties

spring.application.name=lms
spring.datasource.url=jdbc:mysql://localhost:3306/online_learning_school
spring.datasource.username=root
spring.datasource.password=soham
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server port configuration
server.port=8080

logging.file.name=application.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n

# Redis configuration
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.type=redis
#spring.cache.redis.time-to-live=3600000 # 1 hour in milliseconds
